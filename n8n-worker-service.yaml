apiVersion: v1
kind: Service
metadata:
  name: n8n-worker-service
  namespace: default
  labels:
    app: n8n-worker
    component: service
spec:
  type: NodePort
  selector:
    app: n8n-worker
    component: deployment
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 5678