apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: default
  labels: &labels
    app: redis
    component: service
spec:
  type: NodePort
  selector:
    app: redis
    component: statefulset
  ports:
  - name: postgres
    port: 6379
    targetPort: 6379